<div class="page-header">
  <h1>
    Cases
    <a href="/cases/new" class="btn btn-lg btn-success">New Case</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Case type</th>
        <th>Leader</th>
        <th>Industry</th>
        <th>Page</th>
        <th>User</th>
        <th>Actions</th>
      </tr>

      <% @cases.each do |case| %>
      <tr>
        <td><%= case.case_type %></td>
        <td><%= case.leader %></td>
        <td><%= case.industry %></td>
        <td><%= case.page %></td>
        <td><%= case.user_id %></td>
        <td>
          <a href="/cases/<%= case.id %>" class="btn btn-primary">Show</a>
          <a href="/cases/<%= case.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_case/<%= case.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
